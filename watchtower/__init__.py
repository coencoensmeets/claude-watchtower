"""claude-watchtower's Python package.

Right now this holds only the build step. The rest of the panel still lives in
server.py at the repository root and moves here a section at a time — see
docs/cleanup-plan.md.
"""
